// fill an array with 7 values = 0
let table = [];

for(let i =0 ; i < 7; i++) {
    table [i] = 0;
    // or table.push(0)
}

console.table(table);

// From the last array, show the values inside it

for( i =0 ; i < 7; i++) {
    console.log(table[i]);
}
